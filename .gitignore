node_modules
_site
_app
dist
ignore
.env
graphify-out
.agents
.claude
skills-lock.json
documents
cv
cover_letters

# Identity cards for LinkedIn and GitHub. Generated by `npm run cards` and uploaded by hand —
# neither platform has an API for a banner, so they are exports rather than build output.
# og-cover.png IS committed, because the site serves it.
dist-cards/

# Temp pages the card renderer writes beside its template. Cleaned up in a finally, ignored
# as well, because a Chrome that dies hard should not be able to leave one in a commit.
scripts/cards/.render-*.html
