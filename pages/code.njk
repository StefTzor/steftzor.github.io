---
layout: base.njk
permalink: /code/
title: "Code & Architecture | Stefanos Tzortzoglou"
description: "Review the technical stack, Open Source philosophy, and live GitHub telemetry of Stefanos Tzortzoglou."
personSchema: true
mainClass: "flex-grow container mx-auto py-16 px-4"
pageStyle: |
  /* Contribution grid. The data is fetched at build time (_data/contributions.js) and the
     markup is ours, so this styles real elements rather than overriding a vendor stylesheet. */
  .contrib { --gap: 3px; --min-cell: 10px; }
  /* Two columns, two rows: weekday axis and month row laid out against the grid itself, so
     they line up by construction rather than by a matched offset. */
  .contrib-layout { display: grid; grid-template-columns: auto minmax(0, 1fr);
    column-gap: .5rem; row-gap: .35rem; }
  /* Columns are fractions, not a fixed pixel size, so the year fills the card the way the old
     widget's responsive mode did. minmax keeps a floor: below it the grid overflows and the
     card scrolls horizontally rather than shrinking the days into specks. */
  .contrib-grid { display: grid; grid-auto-flow: column;
    grid-template-columns: repeat(53, minmax(var(--min-cell), 1fr));
    grid-template-rows: repeat(7, auto); gap: var(--gap); }
  /* The day colours moved to src/input.css: the app's home view draws the same ramp in a
     twelve-week grid, and two copies of a colour scale is one copy too many. The layout below
     stays here, because a year with axes and a twelve-week strip are different shapes. */
  /* Seven equal tracks over the same height as the grid beside it, so each label sits on the
     weekday it names whatever size the cells end up. Sticky keeps the axis readable when the
     grid is wider than the card. */
  .contrib-days { display: grid; grid-template-rows: repeat(7, 1fr); gap: var(--gap);
    align-self: stretch; position: sticky; left: 0; z-index: 1;
    background: rgb(var(--color-surface)); font-size: .6875rem; }
  .contrib-days span { display: flex; align-items: center; justify-content: flex-end; }
  /* Same column track as the grid, or the labels drift off the weeks they name. They are wider
     than one column and overflow right on purpose - only every fourth or fifth carries text. */
  .contrib-months { display: grid;
    grid-template-columns: repeat(53, minmax(var(--min-cell), 1fr));
    gap: var(--gap); font-size: .6875rem; }
  .contrib-months span { white-space: nowrap; }
sitemapOrder: 40
sitemapChangefreq: weekly
sitemapPriority: "0.8"
---
    <div class="text-center max-w-3xl mx-auto mb-16">
        <h1 class="text-4xl md:text-5xl font-bold mb-6">The Code Behind the <span class="text-brand-accent">Commercial</span></h1>
        <p class="text-lg text-brand-muted leading-relaxed">
            My day job is revenue retention and enterprise implementation. This page is the other half: the
            stack I work in, the tools I reach for, and what I have been committing.
        </p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 max-w-6xl mx-auto">

        <div class="bg-brand-surface p-8 rounded-2xl border border-brand-border">
            <h2 class="text-2xl font-bold mb-6 flex items-center gap-3">
                {% icon "fa-solid-terminal", "text-brand-accent" %} Philosophy & Stack
            </h2>

            <div class="prose dark:prose-invert text-brand-muted leading-relaxed mb-8">
                <p class="mb-4">
                    <strong>I am a pragmatist.</strong> I run a Linux environment because it forces a logical
                    understanding of the system, and I advocate for Open Source tools because transparency
                    beats proprietary black boxes.
                </p>
                <p class="mb-4">
                    I map CRM/ERP systems in raw <strong>SQL</strong> rather than through GUI tools, and work
                    with <strong>Snowflake</strong> on the data side. For campaign execution, I write and hand-tune
                    the HTML behind client email templates.
                </p>
                <p class="mb-4">
                    When troubleshooting <strong>API integrations</strong>, I live in Swagger, OpenAPI, and
                    Insomnia, isolating payload errors long before they require an Engineering escalation.
                </p>
                <p>
                    This portfolio is built on a fast, lightweight stack
                    (11ty, Nunjucks, Tailwind CSS) and deployed via GitHub Actions. It features a custom Firebase
                    authentication flow for locked content. The Firebase web config is public by design, as it is in
                    every Firebase web app, so access is enforced where it actually counts: server-side Firestore
                    security rules that let an account read only its own record and reserve approval to the owner.
                    For backend utilities and data formatting, I default to JavaScript and Python.
                </p>
            </div>

            <div class="grid grid-cols-4 sm:grid-cols-5 gap-6 text-center">
                <div class="group flex flex-col items-center gap-2">
                    {% icon "linux", "w-9 h-9 text-yellow-600 dark:text-yellow-500 group-hover:text-brand-muted transition-colors duration-300" %}
                    <span class="text-xs opacity-0 group-hover:opacity-100 transition-opacity">Linux</span>
                </div>
                <div class="group flex flex-col items-center gap-2">
                    {% icon "azuresqldatabase", "w-9 h-9 text-blue-600 dark:text-blue-400 group-hover:text-brand-muted transition-colors duration-300" %}
                    <span class="text-xs opacity-0 group-hover:opacity-100 transition-opacity">SQL</span>
                </div>
                <div class="group flex flex-col items-center gap-2">
                    {% icon "fa-solid-snowflake", "text-4xl text-blue-500 dark:text-blue-300 group-hover:text-brand-muted transition-colors duration-300" %}
                    <span class="text-xs opacity-0 group-hover:opacity-100 transition-opacity">Snowflake</span>
                </div>
                <div class="group flex flex-col items-center gap-2">
                    {% icon "terraform", "w-9 h-9 text-purple-600 dark:text-purple-400 group-hover:text-brand-muted transition-colors duration-300" %}
                    <span class="text-xs opacity-0 group-hover:opacity-100 transition-opacity">Terraform</span>
                </div>
                <div class="group flex flex-col items-center gap-2">
                    {% icon "javascript", "w-9 h-9 text-yellow-500 dark:text-yellow-300 group-hover:text-brand-muted transition-colors duration-300" %}
                    <span class="text-xs opacity-0 group-hover:opacity-100 transition-opacity">JS</span>
                </div>
                <div class="group flex flex-col items-center gap-2">
                    {% icon "python", "w-9 h-9 text-blue-600 dark:text-yellow-400 group-hover:text-brand-muted transition-colors duration-300" %}
                    <span class="text-xs opacity-0 group-hover:opacity-100 transition-opacity">Python</span>
                </div>
                <div class="group flex flex-col items-center gap-2">
                    {% icon "html5", "w-9 h-9 text-orange-600 dark:text-orange-500 group-hover:text-brand-muted transition-colors duration-300" %}
                    <span class="text-xs opacity-0 group-hover:opacity-100 transition-opacity">HTML</span>
                </div>
                <div class="group flex flex-col items-center gap-2">
                    {% icon "insomnia", "w-9 h-9 text-purple-700 dark:text-purple-500 group-hover:text-brand-muted transition-colors duration-300" %}
                    <span class="text-xs opacity-0 group-hover:opacity-100 transition-opacity">Insomnia</span>
                </div>
                <div class="group flex flex-col items-center gap-2">
                    {% icon "git", "w-9 h-9 text-orange-600 dark:text-orange-500 group-hover:text-brand-muted transition-colors duration-300" %}
                    <span class="text-xs opacity-0 group-hover:opacity-100 transition-opacity">Git</span>
                </div>
                <div class="group flex flex-col items-center gap-2">
                    {% icon "eleventy", "w-9 h-9 text-slate-800 dark:text-gray-200 group-hover:text-brand-muted transition-colors duration-300" %}
                    <span class="text-xs opacity-0 group-hover:opacity-100 transition-opacity">11ty</span>
                </div>
            </div>
        </div>
        <div class="space-y-8">

            <div class="bg-brand-surface p-1 rounded-2xl border border-brand-border shadow-lg overflow-hidden">
                <img width="467" height="195" src="https://steftzor-github-readme-stats.vercel.app/api?username=steftzor&show_icons=true&hide_border=true&bg_color=0f172a&title_color=10b981&icon_color=3b82f6&text_color=94a3b8"
                    alt="GitHub statistics for steftzor: total stars, commits, pull requests, issues and contributions. Full detail on the GitHub profile linked below." class="hidden dark:block w-full h-auto">
                <img width="467" height="195" src="https://steftzor-github-readme-stats.vercel.app/api?username=steftzor&show_icons=true&hide_border=true&bg_color=e2e8f0&title_color=059669&icon_color=3b82f6&text_color=334155"
                    alt="" role="presentation" class="block dark:hidden w-full h-auto">
            </div>

            <div class="bg-brand-surface p-1 rounded-2xl border border-brand-border shadow-lg overflow-hidden">
                <img width="300" height="165" src="https://steftzor-github-readme-stats.vercel.app/api/top-langs/?username=steftzor&layout=compact&hide_border=true&bg_color=0f172a&title_color=10b981&text_color=94a3b8&icon_color=3b82f6"
                    alt="Most-used programming languages across steftzor's public GitHub repositories, by share of code." class="hidden dark:block w-full h-auto">
                <img width="300" height="165" src="https://steftzor-github-readme-stats.vercel.app/api/top-langs/?username=steftzor&layout=compact&hide_border=true&bg_color=e2e8f0&title_color=059669&text_color=334155&icon_color=3b82f6"
                    alt="" role="presentation" class="block dark:hidden w-full h-auto">
            </div>

        </div>

    </div>
    {# --- what the portfolio actually runs -------------------------------------
       The app behind app.tzortzoglou.eu is the strongest technical evidence on this site, and
       until now it was only visible to people with an account. The drawing is shared with the
       app's own /docs/ page rather than copied. #}
    <div class="mt-16 max-w-6xl mx-auto w-full">
        <h3 class="text-xl font-bold mb-6 flex items-center gap-2 text-brand-text">
            {% icon "fa-solid-network-wired", "text-brand-accent" %} A system I run end to end
        </h3>

        <div class="bg-brand-surface border border-brand-border p-6 sm:p-8 rounded-xl">
            <p class="text-brand-muted leading-relaxed max-w-3xl">
                The signed-in app on this domain authenticates people, proxies four third-party APIs,
                writes to Postgres and deploys itself. I designed it, built it and keep it running. That is the same work as onboarding a customer onto a platform: making
                systems that were never built for each other agree on what a field means.
            </p>

            <div class="mt-8 overflow-x-auto">
                {% include "chrome/architecture.njk" %}
            </div>

            <div class="mt-10 grid gap-6 md:grid-cols-2">
                <div>
                    <h4 class="font-bold text-brand-text">Four APIs, twenty-six fields, one shape</h4>
                    <p class="mt-2 text-sm text-brand-muted leading-relaxed">
                        Weather, Swedish transit, Formula 1 and GitHub Actions each answer in their own
                        vocabulary. Twenty-six fields are mapped into one internal shape. The calculated
                        ones (a delay in minutes, a position gained, which step of a build failed) are
                        labelled as calculated, so nobody reads a derivation as a measurement.
                    </p>
                </div>

                <div>
                    <h4 class="font-bold text-brand-text">A two-hour error that was not the vendor's fault</h4>
                    <p class="mt-2 text-sm text-brand-muted leading-relaxed">
                        Every bus departure showed two hours out. The transit feed sends Swedish
                        wall-clock time with no offset and the container runs in UTC, so the times
                        parsed as the wrong instant. They are resolved against Europe/Stockholm now,
                        with tests across both daylight-saving changeovers. The vendor's data was
                        correct the whole time.
                    </p>
                </div>

                <div>
                    <h4 class="font-bold text-brand-text">Authorisation that does not trust its own token</h4>
                    <p class="mt-2 text-sm text-brand-muted leading-relaxed">
                        A signed token proves who signed in. It does not prove what they may do now, so
                        every request re-reads the role and approval state from the database before
                        anything happens. Withdrawing someone's access takes effect on their next
                        request rather than whenever their token happens to expire.
                    </p>
                </div>

                <div>
                    <h4 class="font-bold text-brand-text">A database browser with no query box</h4>
                    <p class="mt-2 text-sm text-brand-muted leading-relaxed">
                        Administrators can read the tables. There is nowhere to type SQL, the readable
                        tables are an allowlist rather than whatever the catalogue reports, and the
                        columns holding a sender's IP address are never selected at all. Losing this screen to a
                        stolen session costs a read, not a prompt.
                    </p>
                </div>
            </div>

            <p class="mt-8 text-sm text-brand-muted">
                The full reference lives inside the app: every endpoint, every field mapping, and why
                each cache window is the length it is. It is at
                <a href="https://app.tzortzoglou.eu/docs/" class="text-brand-accent underline hover:no-underline">app.tzortzoglou.eu/docs</a>.
            </p>
        </div>
    </div>

    <div class="mt-16 max-w-6xl mx-auto w-full">
        <h3 class="text-xl font-bold mb-6 flex items-center gap-2 text-brand-text">
            {% icon "fa-brands-github", "text-brand-accent" %} Live code activity
        </h3>
        {% if contributions %}
<figure class="contrib bg-brand-surface border border-brand-border p-6 rounded-xl shadow-lg w-full overflow-x-auto">
    <figcaption class="sr-only">
        {{ contributions.total }} GitHub contributions between {{ contributions.from }} and
        {{ contributions.to }}, shown as a grid of days shaded by how many contributions each
        holds, darker squares meaning more.
    </figcaption>
    <div class="contrib-layout text-brand-muted">
        <div></div>
        <div class="contrib-months" aria-hidden="true">
            {%- for month in contributions.months %}
            <span>{{ month }}</span>
            {%- endfor %}
        </div>
        <div class="contrib-days" aria-hidden="true">
            <span></span><span>Mon</span><span></span><span>Wed</span><span></span><span>Fri</span><span></span>
        </div>
        <div class="contrib-grid">
            {%- for week in contributions.weeks %}{% for day in week %}
            {%- if day %}<div class="contrib-day" data-level="{{ day.level }}" title="{{ day.label }}"></div>
            {%- else %}<div></div>
            {%- endif %}
            {%- endfor %}{% endfor %}
        </div>
    </div>
    {# A key, because a colour ramp with nothing naming its ends is five shades of nothing. The
       convention is borrowed from GitHub and widely read, which is an argument for keeping the
       ramp and not for leaving it unlabelled - a reader who has never seen one of these grids
       has no way in.

       The whole paragraph is aria-hidden, not just the swatches, because "Fewer" and "More" are
       positional labels: read out without the five boxes between them they are the string
       "Fewer More", which is worse than silence. What they mean is already carried twice in
       language - by the sentence below ("a darker square being a busier day") and by the
       figure's own sr-only caption ("darker squares meaning more") - so nothing is lost by hiding the row.
       The caption used to say only that the grid was "shaded by how many contributions each
       holds", which is the encoding without its direction; the three words were added so that
       this paragraph's claim of twice is true, and so that editing the visible sentence cannot
       take the direction away from a screen reader entirely. It is the direction
       of the ramp that is repeated, not these two words.

       The same key is under the hour grid on /admin/analytics/, drawn from this same ramp. #}
    <p class="mt-4 flex items-center gap-2 text-xs text-brand-muted" aria-hidden="true">
        <span>Fewer</span>
        <span class="flex gap-[3px]">
            <span class="contrib-day h-3 w-3"></span>
            <span class="contrib-day h-3 w-3" data-level="1"></span>
            <span class="contrib-day h-3 w-3" data-level="2"></span>
            <span class="contrib-day h-3 w-3" data-level="3"></span>
            <span class="contrib-day h-3 w-3" data-level="4"></span>
        </span>
        <span>More</span>
    </p>
    <p class="mt-3 text-sm text-brand-muted">
        {{ contributions.total }} contributions in the last year, a darker square being a busier
        day.
        <a href="{{ contributions.profile }}" target="_blank" rel="noopener noreferrer"
            class="text-brand-accent underline hover:no-underline">See the profile on GitHub</a>.
    </p>
</figure>
{% else %}
<p class="bg-brand-surface border border-brand-border p-6 rounded-xl text-brand-muted">
    The contribution data could not be fetched for this build.
    <a href="https://github.com/steftzor" target="_blank" rel="noopener noreferrer"
        class="text-brand-accent underline hover:no-underline">See the profile on GitHub</a>.
</p>
{% endif %}
    </div>
